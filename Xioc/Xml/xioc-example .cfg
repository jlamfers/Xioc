bind(servtype,impltype,transient);
decorate(servtype,impltype,transient);
bindXiocExports(path);
bindMefExports(path);
onNewScope(1,
   bind(servtype,impltype,transient);
);

if(username(jan|piet|klaas),
  if_any_role(a|b|c)
);

if-debug(
);

if-all-roles(admin|user,
  bind(ILogger,Logger)
)
